# PROGRESS

Running log of completed build tasks (appended by the loop, one line per task). Empty = nothing built yet.

<!-- loop appends below this line -->
- Phase 0: Scaffold repo layout (engine/, schema/, definitions/, content-sources/, instruments/, viewer/, test/, vendor/ + package.json with `npm test`, .gitignore, placeholder run-gates.js) — done (gates green)
- Phase 0: Write schema/instrument-definition.schema.json (draft-07; provenance fields + 5 section types: info, scored-likert, self-rating, select-and-commit, free-reflection; scored-likert carries anchors, fractional criterion-referenced band_thresholds, items[text/scale/reverse/points], scales with construct_explainer + per-band light/shadow/one_thing_to_try) — done (gates green; JSON validated, Ajv enforcement lands with harness task)
- Phase 0: Write schema/vault-record.schema.json (draft-07; no-PII top-level lock via additionalProperties:false; fields instrument_id/version, passed-in timestamp, scores/bands/readout [empty for aspirational], student_snapshot; variant enum full|scores-only with if/then/else requiring raw_responses only for full) — done (gates green; JSON + variant logic validated, Ajv enforcement lands with harness task)
- Phase 0: Write test/lint-essence.js (importable ESM + CLI; 11 second-person essence/verdict regexes — "you are", "you’re", "defines you", "the real you" etc.; recursive scan of all strings in definitions/*.json; first-person IPIP item wording safe; handles empty dir) — done (gates green; 8-case self-test passes: flags verdicts, allows tendency language)
- Phase 0: Vendor dependency-free JSON Schema validator (vendor/jsonschema.js — draft-07 subset: $ref/$defs, type/const/enum, string/number/array/object constraints, allOf/anyOf/oneOf/not, if/then/else; chosen over npm-installing Ajv to keep project self-contained, no-build, no-network) — done (12-case self-test vs both real schemas passes: oneOf discrimination, variant if/then/else, additionalProperties PII lock)
- Phase 0: Define fixture format (test/FIXTURE-FORMAT.md — per-instrument cases: 0-based anchor-index responses keyed by item id or "<section>#<idx>", hand-derived expect.scores/bands) AND write test/run-gates.js (8 gates: schema:definitions, schema:records, scoring:fixtures[SKIP until Phase 1], lint:essence, citation:license, render:smoke[SKIP until app files], net:none, pii:none; green-on-empty via built-in sample records + graceful skips; network-mention escape hatch). Caught+fixed a real PII-gate bug: \b word-boundaries miss underscore-joined keys like student_name — replaced with a tokenizing isPiiKey() that catches identity compounds but allows scale_name/instrument_id. — done (full suite green; negative paths verified for validator, linter, PII, network)
- Phase 0 COMPLETE: npm test runs run-gates.js, green on empty/sample setup. Contracts + harness + rails locked before Phase 1.
- Phase 1: engine/scoring.js (PURE ESM, no DOM/IO; reverseKey, responseKey "<section>#<idx>", assignBand [>= cut moves up], scoreInstrument: 0-based anchor-index responses → reverse-keying → sum to facets → roll facets up to parent domains → criterion-referenced bands over each scale's own 0..max range; throws on missing/out-of-range response). Wired the scoring:fixtures gate to actually run cases through the engine + added deepEqualObj. Committed a permanent engine regression test: test/_toy-definition.json (2 facets→1 domain, reverse-keyed items) + test/fixtures/_toy.json (4 hand-derived cases: all-high/all-low/balanced/mixed). — done (full suite green, 0 skipped; engine self-test verified reverseKey, boundary banding, domain rollup, error path; toy def validates + essence-clean)
- Phase 1: engine/sections/* — five renderers (info, scored-likert, self-rating, select-and-commit, free-reflection) + shared engine/dom.js hyperscript helper. Uniform contract render(section, ctx)->{el, read()}; DOM touched only inside render (getDoc resolves ctx.doc|global|throw) so all import cleanly in Node. Accessible: fieldset/legend per item, <label for>, native radios/checkboxes/textarea for keyboard+SR. scored-likert reuses scoring.js responseKey (single source of truth); select-and-commit keeps data-ib-attribute + enforces max_select; free-reflection emits snapshot when is_snapshot. — done (full suite green; 14-assertion fake-DOM self-test verified render + read() collection for all five, incl. missing-response reporting, max_select disabling, min_select, snapshot extraction)
- Phase 1: engine/save-adapter.js — save seam. PURE filenameFor (→ "<id>_YYYY-MM-DD.json") + serializeRecord (pretty JSON). MANUAL path: manualDownload (Blob+anchor download, browser globals injectable via ctx.doc/URL/Blob), copyForOneNote (navigator.clipboard, ctx-injectable), renderSaveControls ("Save my result" + "Copy for OneNote" buttons + aria-live confirmation). GRAPH path: graphSave() is a stub that THROWS "wire in setup session" (no network primitive, so net gate stays green). save(method,...) dispatcher. — done (full suite green incl. net:none over the stub; 13-assertion self-test: pure helpers, fake-DOM download click/revoke, clipboard true/false, graph stub throws, dispatcher)
- Phase 1: engine/engine.js — orchestrator + read-out/reflection model. PURE Node-testable core: buildReadout(def, bands) [teach blocks in declaration order, tendency copy from definition] and assembleRecord(def, parts) [vault record; full includes raw_responses, scores-only omits; timestamp passed in]. createEngine(def, ctx) controller: renders <form> of all sections, collect() merges section reads (responses+snapshot+missing), score(), renderReadout() DOM, buildRecord() collect→score→readout→record (throws if incomplete). Engine never authors prose about the person — only displays definition copy + the student's own snapshot. — done (full suite green, 8 engine modules import clean; 13-assertion self-test incl. end-to-end toy sitting whose assembled record VALIDATES against vault-record schema, incomplete-sitting guard, scores-only variant)
- Phase 1: Engine carries scores-only flag — createEngine(def, ctx, {scoresOnly}) sets the default record variant (scores-only omits raw_responses per DPO escape hatch); explicit buildRecord({variant}) still overrides; flag exposed as engine.scoresOnly. — done (full suite green; 8-assertion test: default→full, flag→scores-only [both schema-valid], explicit override wins)
- Phase 1 COMPLETE: engine (scoring + 5 renderers + orchestrator + save-adapter + scores-only flag) all built & tested; acceptance substantively met (toy runs end-to-end, fixtures pass, no network, records validate). Real on-device browser render = Layer-2.
- Phase 2: learner-profile — built instruments/learner-profile.html (thin, via mountInstrument). — done (full suite green; render:smoke 4 HTML pages). ⇒ learner-profile instrument COMPLETE.
- Phase 4 COMPLETE / TERMINAL: generated comprehensive REVIEW.md (Layer-1 status table; per-instrument source/citation/license + transcribe-check pointers; blocked instruments bigfive+strengths with ready sources; Layer-2 checklist: content fidelity, psychology/tone, subversive-voice, band-threshold + strengths-scope design decisions, DPO/GDPR, live Graph+Azure+SharePoint setup, on-device/HTTPS+JSON-module note; repo map). All BUILD.md tasks checked (0 unchecked). Definition of Done holds: all Layer-1 gates green, every definition has source+citation+license, essence-clean, zero network, no PII, REVIEW.md generated. Loop STOPPED — no further wakeup. Output: DONE — awaiting human Layer-2 review.
- Phase 4: built index.html launcher (links 4 live instruments + viewer; "Coming soon" honestly lists bigfive + strengths as pending content sign-off; relative links, viewport, no network). Confirmed full npm test green across all built instruments (9 gates; render:smoke 6 HTML pages; net:none 17 files). Checked off launcher + full-test boxes. — done. NEXT (final): finalize the comprehensive REVIEW.md Layer-2 handoff, confirm Definition of Done, output "DONE — awaiting human Layer-2 review", STOP the loop.
- Phase 3 COMPLETE: built viewer/viewer.html — loads result files via local file input + FileReader (NO fetch/network), runs aggregateRecords, renders portrait: per-scale longitudinal trajectory tables + bookend Δ for scored instruments, commitments for aspirational, all student snapshots verbatim ("In your own words"); 0-record empty state. Extended render:smoke gate to also check viewer/*.html viewport. Both Phase 3 checkboxes ticked. — done (full suite green, 9 gates). NEXT: Phase 4 — index.html launcher + final full gate run + generate REVIEW.md.
- Phase 3: viewer aggregation core — viewer/viewer.js (PURE, no DOM): aggregateRecords(records)→portrait {instruments[{scored, sittings, first/latest, longitudinal:{scale:[{ts,score,band}]}}], snapshots[], commitments[], total}; scaleChange() for bookend delta. Aspirational surfaced as commitments (array-valued raw_responses), scored as per-scale trajectory; every student_snapshot surfaced verbatim; handles 0..N + mixed + multiple sittings. Generated 5 faithful sample records into test/records/ via the engine (incl. 2 growth-mindset sittings DP1 fixed-lean→DP2 growth-lean for longitudinal). Added viewer:aggregate gate (now 9 gates). — done (full suite green; 11-assertion self-test incl. longitudinal bookend, commitments, chronological snapshots, empty-portrait edge; sample records also boost schema:records→8 + pii:none→8). NEXT: viewer/viewer.html (DOM, file-input load — no fetch) + then Phase 3 robustness checkbox.
- Phase 2 COMPLETE (autonomous scope): 4 instruments fully built end-to-end (grit, self-efficacy, growth-mindset scored; learner-profile aspirational/unscored) — each: cited source → definition → fixture (n/a for learner-profile) → mobile page, all Layer-1 gates green. 2 instruments BLOCKED & documented in REVIEW (bigfive IPIP-NEO-120, strengths IPIP-VIA): sources real+public-domain but item sets too large to transcribe verbatim via tooling without fabricating; await human content-fidelity transcription. Phase 2 BUILD checkboxes checked with block notes. NEXT: Phase 3 — viewer.
- Phase 2: learner-profile — authored definitions/learner-profile.json (UNSCORED aspirational; 13 sections: intro info + 10 per-attribute subversive-then-constructive info reframes + select-and-commit [10 options each mapped to ib_attribute, min 1/max 3] + free-reflection snapshot). Reframes authored in-house (DESIGN), essence-clean. — done (full suite green, 4 definitions; inline check: validates, essence-clean, unscored end-to-end record has empty scores/bands/readout + captures commitments in raw_responses + snapshot, record validates). Subversive-voice review flagged in REVIEW. NEXT for learner-profile: build instruments/learner-profile.html (no fixture — unscored).
- Phase 2: learner-profile content-source — content-sources/learner-profile.md. Aspirational/UNSCORED; distinct from psychometric instruments: the subversive-then-constructive reframes are LOOP-AUTHORED pedagogical content (DESIGN assigns this to the loop), grounded in the 10 official IB Learner Profile attributes (the sourced framework). Recorded all 10 attribute names (confirmed) + descriptors (4 verbatim from search, 6 paraphrased pending ibo.org which 403s automated fetch) + IBO 2017 citation. Flagged in REVIEW: human subversive-voice/tone review + verify official descriptors + IB trademark usage. Section plan documented (info reframes + select-and-commit w/ ib_attribute mapping + free-reflection snapshot). — done (gates green). NEXT for learner-profile: author definitions/learner-profile.json (authorable; no fixture since unscored) + html.
- Phase 2: strengths (IPIP-VIA, public domain) content-source — ⛔ BLOCKED (not fabricated). Confirmed real public-domain source (ipip.ori.org/newVIAKey.htm; 24 strengths / ~213 items; Peterson & Seligman framework, IPIP analog per Operating Rule 3 = not VIA proprietary) and recorded provenance in content-sources/strengths.md. Fetch returns only ~170-180 of 213 items (omissions + version caveats) → cannot transcribe verbatim autonomously without fabricating. ALSO an unresolved design decision (all 24 strengths vs reduced subset) = human's call (Operating Rule 8). Logged in REVIEW. No definitions/strengths.json. — gates green (nothing fabricated). NEXT: learner-profile (aspirational, authorable from published IB attributes).
- Phase 2: growth-mindset — built instruments/growth-mindset.html (thin, via mountInstrument). — done (full suite green; render:smoke 3 HTML pages). ⇒ growth-mindset instrument COMPLETE end-to-end. [3 instruments done: grit, self-efficacy, growth-mindset; bigfive blocked; remaining: strengths (IPIP), learner-profile (aspirational)]
- Phase 2: growth-mindset — hand-derived test/fixtures/growth-mindset.json (6 cases, both band edges pinned). The gate CAUGHT my arithmetic error: I'd expected score 10/15 → growth-lean, but 0.6667*15=10.0005 so 10 is 'mixed' (growth-lean needs ≥11). Engine correct; corrected the fixture (10→mixed, 11→growth-lean) and documented the band-threshold rounding (0.6667 > exact 2/3, so exact-2/3 scores land in the middle band — same as GSE 20/30) in REVIEW for design sign-off. scoring:fixtures now 18 cases / 4 files, green. — done. NEXT for growth-mindset: build instruments/growth-mindset.html.
- Phase 2: growth-mindset — authored definitions/growth-mindset.json (3 items verbatim; single scale growth_mindset; 6-pt anchors agree(0)→disagree(5), reverse:false so high=growth; semantic band ids fixed-lean/mixed/growth-lean; construct_explainer + tendency band copy, essence-clean even with verbatim "you can't change" items; info intro + synthesis). — done (full suite green, 3 definitions; inline check: validates, scores 0/7/15→fixed-lean/mixed/growth-lean, transcribe-check all 3 verbatim). NEXT for growth-mindset: hand-derive fixture + build html.
- Phase 2: growth-mindset (3-item, Dweck implicit theories) content-source — content-sources/growth-mindset.md with 3 VERBATIM fixed-mindset items (SPARQtools/Stanford; search confirmed item 3), 6-pt scale. Tiny by design (DESIGN "movable change-meter"). Scoring convention documented: items are fixed statements scored by disagreement → anchors authored agree(0)→disagree(5) so high index = growth, NO reverse flag; flagged anchor-direction dependency + licensing in REVIEW. — done (gates green). NEXT for growth-mindset: author definitions/growth-mindset.json + fixture + html.
- Phase 2: self-efficacy — built instruments/self-efficacy.html. Factored shared page glue into instruments/instrument-page.js (mountInstrument(definition): builds form + "Show my read-out" + save/copy controls, runs the full flow) so each instrument HTML is truly thin (just imports its JSON-module definition + calls mountInstrument). Refactored grit.html to use it too (no duplicated page script). — done (full suite green: render:smoke 2 HTML pages w/ viewport, net:none 12 files clean). ⇒ self-efficacy (GSE) instrument COMPLETE end-to-end. [grit + self-efficacy both done; bigfive blocked; remaining: growth-mindset, strengths, learner-profile]
- Phase 2: self-efficacy — hand-derived test/fixtures/self-efficacy.json (4 cases, by hand: all-exactly-true 30→high, all-not-true 0→low, all-moderately-true 20→balanced [deliberate near-boundary: 20/30≈0.6667 falls just under cut], low-boundary 9→low). scoring:fixtures now 12 cases / 3 files, green. — done. NEXT for self-efficacy: build instruments/self-efficacy.html.
- Phase 2: self-efficacy — authored definitions/self-efficacy.json (10 items transcribed verbatim from content-source; single forward-keyed scale self_efficacy, no facets/domain; 4-pt anchors; construct_explainer + tendency-language band copy, essence-clean; info intro + free-reflection snapshot). — done (full suite green, 2 definitions; inline check: validates, scores 0/15/30→low/balanced/high, transcribe-check all 10 verbatim). NEXT for self-efficacy: hand-derive fixture + build html.
- Phase 2: self-efficacy (GSE, 10 items) content-source — content-sources/self-efficacy.md with all 10 items transcribed VERBATIM as returned by PsyToolkit (the source actually retrieved; search confirmed items 1–4); single scale, NO reverse items, 4-pt anchors. Caught + corrected my own drift: had initially written canonical FU-Berlin wording for items 3/5/8/9/10 from memory → reverted to the actually-retrieved PsyToolkit text and flagged the canonical-wording discrepancy in REVIEW for human verification against the primary source. License free-for-education (not PD) → DPO flag. — done (gates green). NEXT for self-efficacy: author definitions/self-efficacy.json + fixture + html.
- Phase 2: grit — built instruments/grit.html (thin mobile page) + shared instruments/instrument.css (mobile-first, WCAG-AA: contrast, visible focus, 48px touch targets). Loads engine + grit.json via JSON module import (with{type:json}) — NO fetch/network; flow = answer → "Show my read-out" (validates completeness, teaches bands) → write synthesis → "Save my result"/"Copy for OneNote" (builds fresh record at click, manual download/clipboard). JSON-module Safari 17.2+ requirement flagged in REVIEW for phone test. — done (full suite green: render:smoke validates 1 HTML page w/ viewport, net:none scans it clean). ⇒ grit instrument COMPLETE end-to-end (source→definition→fixture→page); per-instrument acceptance met for all Layer-1 checks.
- Phase 2: grit — hand-derived test/fixtures/grit.json (4 cases computed by hand, NOT from engine output: max-grit 16/16/32 all-high, min-grit 0/0/0 all-low, all-midpoint 8/8/16 all-balanced, uniform-"very much like me" 0/16/16 [exercises reverse-keying: agreeing to distractibility items → low consistency]). scoring:fixtures gate now runs 8 cases across 2 files, green — independent confirmation the engine scores grit correctly. — done (full suite green). NEXT for grit: build instruments/grit.html.
- Phase 2: grit — authored definitions/grit.json (transcribed 8 items verbatim from content-source; 3 scales: consistency_of_interest + perseverance_of_effort facets → grit domain; 5-pt anchors low→high; reverse flags per documented convention [consistency reverse, perseverance forward]; construct_explainer + tendency-language band copy I authored, essence-clean; info intro + free-reflection synthesis snapshot). — done (full suite green: schema+citation+essence all pass on 1 definition; inline check verified scoring max=32/min=0 with correct facet+domain rollup, and transcribe-check confirmed all 8 item texts appear verbatim in content-sources/grit.md). NEXT for grit: hand-derive test/fixtures/grit.json + build instruments/grit.html.
- Phase 2: grit (Grit-S, 8 items) content-source — content-sources/grit.md with all 8 VERBATIM items (PsyToolkit, cross-checked vs DMIDI + search snippets), 2 facets (consistency_of_interest items 1/3/5/6 reverse; perseverance_of_effort items 2/4/7/8 forward) rolling up to a `grit` domain, 5-pt anchors, Duckworth & Quinn 2009 citation. License is NOT public domain (© Duckworth, free educational use) → flagged in REVIEW for DPO; keying-direction convention ambiguity documented + flagged. — done (gates green). NEXT for grit: author definitions/grit.json (transcribe) + hand-derive fixture + build instruments/grit.html.
- Phase 2: bigfive (IPIP-NEO-120) content-source — ⛔ BLOCKED (not fabricated). Found the real public-domain source (ipip.ori.org item key, Johnson 2014, OSF scoring key) and recorded full provenance in content-sources/bigfive.md, but the fetch tool returns only a representative item sample, not all 120 verbatim with per-item facet+keying. Per Operating Rule 3 (transcribe, never invent) did NOT author items from memory; logged as a human content-fidelity task in REVIEW.md (started). No definitions/bigfive.json created. — gates green (nothing fabricated). NEXT: move to a verbatim-sourceable instrument (grit-S / GSE).
- POST-LOOP (user request — reconstruct blocked instruments from multiple sources): BIG FIVE UNBLOCKED & built end-to-end. Cross-checked 2 independent public-domain repos (Alheimsins b5-johnson-120 data/en/questions.json + NeuroQuestAi five-factor-e) — 119/120 items identical, 1 one-word O6 variant (used canonical "and"); sampled vs ipip.ori.org. Built content-sources/bigfive.md (full verbatim 120 items by facet+keying), definitions/bigfive.json (120 items→30 facets→5 domains, reverse=keyed-minus, authored tendency band copy for all 35 scales), test/fixtures/bigfive.json (3 rule-derived cases incl. all-mid→every facet 8/domain 48), instruments/bigfive.html, promoted in index.html. — full suite green (5 definitions, 21 fixture cases, 7 pages). TODO: rebuild REVIEW.md after Strengths; flag bigfive band copy (35 scales) for Layer-2 psych/tone review. NEXT: Strengths (IPIP-VIA).
- POST-LOOP: STRENGTHS UNBLOCKED & built end-to-end. Fetched all 24 IPIP-VIA strengths verbatim from ipip.ori.org/newVIAKey.htm in scale-grouped batches (213 items, counts match page); transcribed to .work/via-items.json; built definitions/strengths.json (213 items → 24 strengths → 6 VIA virtues as parents; reverse=keyed minus; authored tendency band copy for all 30 scales — essence linter caught 3 of my own "you are"/"the real you" slips, fixed). content-sources/strengths.md (full verbatim by virtue→strength + single-source caveat), test/fixtures/strengths.json (3 rule-derived cases), instruments/strengths.html, promoted in index.html (Coming-soon section removed). — full suite green: 6 definitions, 24 fixture cases / 6 files, 8 pages, 19 net-clean files.
- POST-LOOP COMPLETE: both blocked instruments resolved. ALL 6 instruments built & gate-green. Rewrote REVIEW.md (both built; Layer-2 now centres on verifying Strengths' 213 single-source items + reviewing the ~65 scales of authored band copy across Big Five + Strengths). Added .work/ to .gitignore. Still DONE — awaiting human Layer-2 review.
- POST-LOOP (user feedback: automate the checking, drop GDPR/on-device, give Azure steps): Added content:fidelity GATE (10 gates now) — verifies all 354 scored items appear verbatim in their content-sources on every npm test (transcribe-check now automated, not a human chore). Attempted a 2nd independent source for Strengths (none exists; original IPIP-VIA is canonical to ipip.ori.org) — counts match source exactly + gate locks drift. Added staticwebapp.config.json (Azure SWA: correct JSON mime for module imports, headers) + DEPLOY.md (Free-tier Azure Static Web Apps, GitHub + no-git CLI routes). Rewrote REVIEW.md lean: dropped GDPR (on-site IT) + on-device/file:// (moot on Azure HTTPS); stated design decisions as made; the one real human item = read-through of the ~65 scales of authored band copy. All gates green.
- FIX (file:// CORS — pages failed to load when opened directly): ES-module imports + JSON-module loading are blocked from file:// origins. Added build/bundle.mjs: generates app.bundle.js (classic IIFE → window.PI; registry-shim strips import/export from the ES sources; self-checks no import/export/fetch) and regenerates all 6 instrument pages (classic <script src=app.bundle.js> + inlined definition JSON, no module/fetch) + viewer.html (classic + FileReader). ES-module sources stay canonical for the Node gates. Added `npm run build`. New gates: net:none now also scans app.bundle.js (20 files); page:sync verifies each page's inlined def == definitions/*.json (catches stale pages). Verified bundle in a Node vm sandbox (window/document): window.PI exposes all fns, scoreInstrument/createEngine/aggregateRecords work. All 11 gates green. Updated DEPLOY.md (pages now file://-safe; runtime fileset = index.html + app.bundle.js + instruments/*.html + instrument.css + viewer.html; run npm run build after editing engine/definitions).
